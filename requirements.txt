# Core
openai>=1.0.0
python-dotenv>=1.0.0

# Embeddings
sentence-transformers>=2.7.0

# BoW and Sparse (TF-IDF) — part of scikit-learn, no extra install needed
# CountVectorizer and TfidfVectorizer are used from sklearn

# Vector store
chromadb>=0.5.0

# Data
pandas>=2.0.0
numpy>=1.26.0

# Evaluation & visualisation
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Serialisation (saving fitted vectorizers)
# pickle is built into Python — no install needed

# Notebook
jupyter>=1.0.0
ipykernel>=6.0.0
